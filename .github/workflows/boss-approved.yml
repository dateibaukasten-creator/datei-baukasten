name: name: Boss Approved Run

on:
  push:
    paths:
      - 'approvals/**'                       # feuert, wenn in /approvals eine Datei entsteht
  workflow_dispatch:                         # zusätzlich: manuell startbar

permissions:
  contents: write

jobs:
  generate:
    # Manuell: immer laufen. Bei push: nur wenn die Commit-Message "boss: go" enthält.
    if: github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, 'boss: go')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Debug – Status anzeigen
        run: |
          echo "Node:"; node -v
          echo "--- Root"; ls -la
          echo "--- ops"; ls -la ops || true
          echo "--- utility-site"; ls -la utility-site || true
          echo "--- seed.json"; cat ops/seed.json || true

      - name: Generator ausführen (fehler-tolerant)
        run: |
          node ops/generator.js || echo "::warning ::Generator ohne neue Seiten oder mit Fallback gelaufen"

      - name: Änderungen committen & pushen
        run: |
          git config user.name  "auto-bot"
          git config user.email "auto-bot@users.noreply.github.com"
          git add -A
          git commit -m "auto: +10 Seiten (Generator)" || echo "nichts zu committen"
          git push
